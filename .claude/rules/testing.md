# 测试约定（每个切片都要带）

后端 pytest（`backend/tests/`）必测：
- 正常路径（返回结构正确）；
- 鉴权守卫（无权限角色 → 403）；
- 入参校验（必填缺失 → 422）；
- 写操作：状态变更 + 审计留痕 +（如有计算）重算正确。

前端 Vitest（`*.test.ts(x)`）必测：
- http mutator 行为（认证头、非 2xx 抛错、401 清 token）已有基线；
- 页面关键状态：至少空态、错误态、否决/无权限隐藏写操作。

要求：新增/改动切片**同 PR 附测试**；`make test-all` 必须全绿才算完成。
