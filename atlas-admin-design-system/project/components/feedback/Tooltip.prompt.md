**Tooltip** — label an icon-only button or explain a truncated/permission-code cell.

```jsx
<Tooltip label="导出为 CSV"><Button variant="ghost" size="icon"><DownloadIcon/></Button></Tooltip>
```

`side`: top (default) / bottom / left / right. Keep the label to a few words.
