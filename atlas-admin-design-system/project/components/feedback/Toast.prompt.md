**Toast** — brief confirmation, usually bottom-right, auto-dismissed.

```jsx
<Toast tone="success" title="保存成功" description="用户信息已更新" onClose={close} />
```

Colored accent bar + dot by tone. Presentational only — manage the queue and timeout in your app.
