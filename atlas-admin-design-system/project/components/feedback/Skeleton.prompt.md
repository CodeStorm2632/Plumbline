**Skeleton** — placeholder while table/detail data loads.

```jsx
<Skeleton circle height={32} />
<Skeleton width={180} />
<Skeleton width="60%" height={12} />
```

Compose several to mirror the final layout. Subtle shimmer; stop rendering once data arrives.
