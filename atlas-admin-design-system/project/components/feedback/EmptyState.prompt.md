**EmptyState** — centered placeholder for empty tables or zero search results.

```jsx
<EmptyState title="暂无用户" description="点击右上角新增第一个用户" action={<Button size="sm">新增用户</Button>} />
```

Pass a custom `icon` to match context; keep copy short and offer the next action.
