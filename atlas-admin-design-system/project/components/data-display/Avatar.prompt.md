**Avatar** — user identity in tables, nav, and the profile menu.

```jsx
<Avatar name="张伟" />
<Avatar src="/u/12.jpg" name="Li Na" size="lg" />
```

Without `src`, shows two-letter initials on a tint derived deterministically from `name`. `size` accepts sm/md/lg or a pixel number.
