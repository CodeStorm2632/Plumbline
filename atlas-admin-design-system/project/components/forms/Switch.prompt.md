**Switch** — instant on/off toggle, typically for a row's 启用/停用 status.

```jsx
<Switch checked={enabled} onChange={setEnabled} />
<Switch size="sm" checked disabled />
```

Fills with indigo primary when on. Use for state that applies immediately; use Checkbox for form selections.
